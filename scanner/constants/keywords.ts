import { TOKEN_TYPES } from "../../tokens/constants/tokensType";

export const RESERVED_TOKEN_KEYWORDS = {
    'and': TOKEN_TYPES.AND,
    'class': TOKEN_TYPES.CLASS,
    'else': TOKEN_TYPES.ELSE,
    'if': TOKEN_TYPES.IF,
    'for': TOKEN_TYPES.FOR,
    'false': TOKEN_TYPES.FALSE,
    'true': TOKEN_TYPES.TRUE,
    'fun': TOKEN_TYPES.FUNCTION,
    'null': TOKEN_TYPES.NULL,
    'or': TOKEN_TYPES.OR,
    'pring': TOKEN_TYPES.PRINT,
    'return': TOKEN_TYPES.RETURN,
    'super': TOKEN_TYPES.SUPER,
    'this': TOKEN_TYPES.THIS,
    'var': TOKEN_TYPES.VAR,
    'while': TOKEN_TYPES.WHILE,
    'print': TOKEN_TYPES.PRINT
}
